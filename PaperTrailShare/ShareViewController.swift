import UIKit

final class ShareViewController: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        // Placeholder: no UI yet, just complete the extension request immediately.
        extensionContext?.completeRequest(returningItems: nil)
    }
}
